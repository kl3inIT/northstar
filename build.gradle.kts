// Root build. Shared configuration is provided by convention plugins in
// `build-logic` (applied per module), so this file stays intentionally thin.

group = "com.northstar"
version = "0.0.1-SNAPSHOT"
