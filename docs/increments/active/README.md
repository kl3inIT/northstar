# Active Increments

Create one directory per active increment:

```text
docs/increments/active/YYYY-MM-DD-slug/
  design.md
  plan.md
```

The active design and plan are authoritative only while the increment is active.
