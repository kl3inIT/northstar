# Study Test Matrix

Reusable testing mechanics live in
[../../guidelines/testing-harness.md](../../guidelines/testing-harness.md).

| Behavior | Coverage | Notes |
| --- | --- | --- |
| FSRS scheduling boundary | Automated | `VocabSchedulerTests` pins the official FSRS-6 wrapper: four distinct new-card outcomes, deterministic fuzzed previews, Review-only lapse classification, leech threshold, and compact interval labels. |
| Writing grade structural checks | Automated | `core/src/test/java/com/northstar/core/study/WritingGraderTests.java` pins the code half of the evaluator loop: half-band steps, range width and ordering, verbatim quote check with whitespace tolerance, missing criteria, empty summary. |
| Skill canonicalization and score-pair validation | Gap | Accent-insensitive skill merge and both-or-neither score validation are exercised only through integration flows; a focused `StudyService` unit test is missing. |
| Vocab create dedupe and review persistence | Partial | `VocabServiceFsrsTests` pins due filtering, preview-version conflict, before/after log write, lapse/relearning, and timezone sibling bury. Dedupe plus a real database review round trip still need consolidated Testcontainers coverage. |
| Vocabulary base metadata | Automated | `ToolSupportTests` pins JSON escaping and omission rules for reading, part of speech, and a user-supplied example. Capture and Assistant contracts require reading/part of speech when known and do not generate optional enrichment on save. |
| Vocabulary coaching validation | Automated | `VocabCoachTests` pins direction-aware semantic-answer validation, corrective retry, requested-field-only enrichment, preservation of unknown metadata keys, and best-effort word formation: valid structured parts persist while absent/malformed decomposition becomes null without failing unrelated enrichment. It also replays the captured production `headquarters` shape to prove uppercase top-level and snake-case family aliases map without changing the canonical provider schema. |
| Vocabulary review REST policy | Automated | `StudyControllerTests` pins that page ratings carry their direction with source `MANUAL`, deck defaults round-trip, synchronous enrichment remains preview-only, and background job endpoints delegate without writing during preview. |
| Vocabulary production memory | Automated + runtime verified | `VocabLanguageDeckTests` pins independent recognition/production FSRS rows, unchanged scheduling on classification edits, and one sibling per due queue snapshot. The 2026-07-12 walkthrough still covers two-direction prompt/listening behavior; the FSRS walkthrough additionally verified a production sibling buried to the next Asia/Bangkok day. |
| Vocabulary background enrichment and images | Automated + runtime verified | `VocabEnrichmentJobServiceTests` pins preview-only start, discard, transactional Apply, review-version tolerance, non-answer-leaking image alt text, and content-change rejection. `VocabCoachTests` pins the closed word-part kind schema and best-effort omission; `SpringAiClientRouterTests` pins deterministic Nine Router options and native-OpenAI token-field compatibility. `OpenAiCompatibleImageGenerationGatewayTests` pins direct OpenAI base64, Nine Router HTTP/1.1 binary transport, raster signature/type, and invalid output. A live OpenAI `gpt-image-2` run on 2026-07-12 generated while the learner advanced to another card, displayed the image at the top of the ready Sheet, and persisted it only on Apply through Attachments. A 2026-07-13 production diagnosis traced one Nine Router failure to a headerless Docker-network EOF; the configured `cx/gpt-5.5-image` route and 9Router 0.5.30 were current, and an isolated live binary probe immediately returned a 491122-byte PNG. Clear-text gateway clients now pin HTTP/1.1 consistently with TTS and catalog calls rather than silently retrying a potentially non-idempotent image POST. |
| Vocabulary audio enrichment and practice history | Automated + runtime verified | `SpeechAssetServiceTests`, `VocabAudioPracticeServiceTests`, `VocabEnrichmentJobServiceTests`, and `StudyControllerTests` pin transient TTS preview, persist-on-Apply source bindings, target-only bilingual example audio, connected-example-only Shadowing with no word fallback, deterministic Dictation diff, 180-day/max-20 retention, recording playback, pin/delete, and successful-assessment-only writes. Web state tests pin the same target extraction and spaced/Han connected-speech boundary, exact-text audio validity, mode reference selection, provider-comparable trends, and semantic Dictation tokens. A live 2026-07-13 pass used configured 9Router TTS and Azure eastasia: word/example preview and Apply succeeded, Word returned 97 accuracy / 100 fluency / 88 prosody with a playable retained recording, the former delayed-repeat Shadowing used the saved example, Dictation covered exact and substitution/missing diffs, and history returned all three modes. Playwright verified explicit background generation, persistent ready action, browser-speech fallback, immediate applied-audio override, production audio hidden until reveal, real recorder-to-Azure assessment, pinning, mode/provider-scoped trends, readable Dictation feedback, and zero console errors or warnings. A follow-up real-browser regression used isolated fixtures plus a synthetic microphone to prove the new target-only sentence, no-example lock, same-turn model/capture, automatic tail stop, replay-ready state, and no pronunciation POST before explicit Save; it also finished with zero console errors/warnings and removed both fixtures. |
| Study/vocab/writing/speaking REST round trip | Gap | No `StudyControllerIntegrationTests` yet; local gates stay light and CI owns the heavier controller/database suite. Speech endpoints were contract-generated and live-verified, but other REST paths still lack one consolidated integration test. |
| Capture STUDY and VOCAB classification | Runtime verified | Live-model E2E on 2026-07-11: one Vietnamese sentence became two sessions with split scores and discipline links; a vocab sentence produced two cards with tone-marked pinyin/IPA under the former prompt. The current contract adds part of speech and deliberately stops automatic example/enrichment generation. Prompt-content assertions in `CaptureServiceIntegrationTests` do not yet cover the study/vocab sections. |
| Assistant definition-to-card routing | Automated + runtime verified | `AssistantStudyPromptTests` pins that a specific foreign-word definition request calls `save_vocab_cards`, honors an explicit no-save request, requires an unambiguous contextual word, and ignores incidental prose/research vocabulary. A live routed-model E2E on 2026-07-12 asked “Sesquipedalian nghĩa là gì?”, called `save_vocab_cards`, and produced the Vietnamese meaning, IPA, example plus English discipline; the card and conversation fixtures were removed afterwards. Re-verify whenever the system prompt or tool descriptions change. |
| grade_writing end-to-end with evaluator loop | Runtime verified | Live-model E2E on 2026-07-11 (three gradings of a planted-error essay): stable 5.0-5.5 range across grader v1/v2, verbatim-quoted justifications, under-length called out, prior-error persistence named in the summary. |
| Grammar-weakness corpus aggregation | Automated + runtime verified | `WritingServiceTests` pins case-insensitive grouping, recency-first ordering, example capping, malformed JSON, empty history, and writing/speaking label collisions. Local MCP E2E on 2026-07-11 confirmed a persisted speaking error appeared in `grammar_weaknesses` beside writing errors. |
| Grammar drill protocol end-to-end | Runtime verified | Live-model E2E on 2026-07-11: "luyện ngữ pháp đi" called `grammar_weaknesses` + `find_study_sessions`, drilled the real SVA/linking patterns one item at a time with rule explanations, and logged Grammar PRACTICE 1/1 with pattern-naming notes on early stop. |
| Assistant tool routing for "chấm bài" | Runtime verified | Before the system-prompt route, the model graded by hand from KB notes and saved nothing; after, a fresh conversation called `grade_writing`. Re-verify when the assistant system prompt changes. |
| OpenAPI required fields and generated web types | Static | Regenerate `contracts/openapi.json`, run `pnpm -C web gen:api`, then `pnpm -C web typecheck`. |
| Speech locale and WAV guards | Automated | `SpeechLocalesTests` pins Han→`zh-CN` and non-CJK→`en-US`; `WavAudioTests` pins the 16-kHz/16-bit/mono contract plus malformed format and duration rejection. |
| Speaking feedback validation and corpus union | Automated | `SpeakingCoachTests` pins whitespace-tolerant verbatim quotes, score bounds, provider-score separation, IELTS evidence in the evaluator envelope, and persistence of the scorer version. `WritingServiceTests` pins union/collision behavior and exact shared top-error semantics. |
| Speaking IELTS-style estimate policy | Automated | `SpeakingEstimatePolicyTests` pins the exact FC/LR/GRA/P set, half-band steps, maximum 1.0 range width, LOW/MEDIUM criterion confidence, transcript-grounded evidence, LOW overall confidence, and deterministic Java aggregation. |
| Azure adapter parsing and aggregation | Automated + captured response | Adapter tests pin configuration modes, continuous paragraph aggregation, error mapping, and parsing against the unmodified real Azure JSON resource `azure-reading-response.json`. No live call runs in CI. |
| Speech live provider flow | Runtime verified | Dev-only E2E on 2026-07-11 with real Azure eastasia credentials and 16-kHz spoken WAVs: card read returned 92 accuracy / 100 fluency / 90.3 prosody; speaking returned a transcript plus 92 pronunciation / 92.29 fluency / 89.7 prosody, persisted provider/model metadata, logged a one-minute Speaking PRACTICE session, and surfaced its verb-form error through local MCP `grammar_weaknesses`. Fixtures were removed afterwards. The API Dockerfile also asserts the Azure native dependencies `libstdc++.so.6` and `libuuid.so.1` exist in the final image; a 2026-07-13 image smoke additionally assessed and served one retained WAV. |
| Speaking composite live flow | Runtime verified | Dev-only E2E on 2026-07-12 used real Azure eastasia delivery assessment and the routed `gpt-5.5` study grader. It persisted and rendered a LOW-confidence 7.0-8.0 overall range with four criterion ranges and evidence while keeping Azure pronunciation 87.49, fluency 83.67, and prosody 89.5 visibly separate. The feedback/session fixtures and temporary WAV were removed afterwards. |
| /study page and Capture study controls | Runtime verified | Playwright walkthroughs on 2026-07-11 at 1440x900 covered Log, Vocabulary, Writing, and the original Speaking flow. A 2026-07-12 live walkthrough additionally verified latest/history/detail IELTS-style ranges, four criteria, expanded evidence, separately-labelled raw scores, and a clean browser console. Capture chips and unified Recent rows remain covered by the earlier walkthrough. |
| Focused vocabulary review flow | Automated + runtime verified | `web/test/vocabulary-review-state.test.mjs` pins front/back/rating/completion transitions, keyboard guards, deck isolation, due/buried calculation, and explicit-only enrichment. On 2026-07-12 a real browser/API/DB pass showed due count 2, Learning previews `1m/6m/10m/12-13d`, successful Good and Easy writes, completion, Review previews `10m/10d/20d/1mo`, and Again persisting Relearning step 0 with a 10-minute due time and lapse count 1. All three writes were HTTP 200 with a clean console. Earlier walkthrough evidence still covers rich enrichment, reversible flipping, production prompts, and compact long-card layout. |
| Vocabulary language and deck scoping | Automated + runtime verified | `VocabLanguageDeckTests` pins deterministic legacy detection, independent language/deck queues, case-insensitive deck canonicalization, General semantics, and unchanged memory state on classification edits. `StudyControllerTests` pins the language/deck arguments. The web state test pins deck filtering and query omission for All decks. A Playwright walkthrough on 2026-07-12 used isolated English General/IELTS and Chinese General/HSK4 fixtures: tabs reset to All decks, stats/table followed scope, and review network calls were exactly `ENGLISH+IELTS` and `CHINESE+HSK4`; console had no errors or warnings. |
| Live model grading agreement | Gap | No examiner-labelled calibration set is wired into an opt-in evaluation for either writing or speaking. Speaking therefore exposes ranges with LOW one-answer confidence; neither Azure delivery values nor literature claims establish IELTS agreement. |
