# Study Test Matrix

Reusable testing mechanics live in
[../../guidelines/testing-harness.md](../../guidelines/testing-harness.md).

| Behavior | Coverage | Notes |
| --- | --- | --- |
| Ebisu memory model math | Automated | `core/src/test/java/com/northstar/core/study/EbisuTests.java` pins the vendored port to analytic values: expected recall at the conjugate point, moment-matched updates, half-life growth/shrink, balanced-model half-life identity, monotonic decay, gammaln reference values, and 30-iteration lapse stability. |
| Writing grade structural checks | Automated | `core/src/test/java/com/northstar/core/study/WritingGraderTests.java` pins the code half of the evaluator loop: half-band steps, range width and ordering, verbatim quote check with whitespace tolerance, missing criteria, empty summary. |
| Skill canonicalization and score-pair validation | Gap | Accent-insensitive skill merge and both-or-neither score validation are exercised only through integration flows; a focused `StudyService` unit test is missing. |
| Vocab create dedupe, at-risk ranking, review logging | Gap | `VocabService` paths (existing-front return, predictRecall ordering, MIN_ELAPSED clamp, before/after log rows) need a Testcontainers integration test. |
| Study/vocab/writing/speaking REST round trip | Gap | No `StudyControllerIntegrationTests` yet; local gates stay light and CI owns the heavier controller/database suite. Speech endpoints were contract-generated and live-verified, but other REST paths still lack one consolidated integration test. |
| Capture STUDY and VOCAB classification | Runtime verified | Live-model E2E on 2026-07-11: one Vietnamese sentence became two sessions with split scores and discipline links; a vocab sentence produced two enriched cards (tone-marked pinyin, IPA, generated examples). Prompt-content assertions in `CaptureServiceIntegrationTests` do not yet cover the study/vocab sections. |
| grade_writing end-to-end with evaluator loop | Runtime verified | Live-model E2E on 2026-07-11 (three gradings of a planted-error essay): stable 5.0-5.5 range across grader v1/v2, verbatim-quoted justifications, under-length called out, prior-error persistence named in the summary. |
| Grammar-weakness corpus aggregation | Automated + runtime verified | `WritingServiceTests` pins case-insensitive grouping, recency-first ordering, example capping, malformed JSON, empty history, and writing/speaking label collisions. Local MCP E2E on 2026-07-11 confirmed a persisted speaking error appeared in `grammar_weaknesses` beside writing errors. |
| Grammar drill protocol end-to-end | Runtime verified | Live-model E2E on 2026-07-11: "luyện ngữ pháp đi" called `grammar_weaknesses` + `find_study_sessions`, drilled the real SVA/linking patterns one item at a time with rule explanations, and logged Grammar PRACTICE 1/1 with pattern-naming notes on early stop. |
| Assistant tool routing for "chấm bài" | Runtime verified | Before the system-prompt route, the model graded by hand from KB notes and saved nothing; after, a fresh conversation called `grade_writing`. Re-verify when the assistant system prompt changes. |
| OpenAPI required fields and generated web types | Static | Regenerate `contracts/openapi.json`, run `pnpm -C web gen:api`, then `pnpm -C web typecheck`. |
| Speech locale and WAV guards | Automated | `SpeechLocalesTests` pins Han→`zh-CN` and non-CJK→`en-US`; `WavAudioTests` pins the 16-kHz/16-bit/mono contract plus malformed format and duration rejection. |
| Speaking feedback validation and corpus union | Automated | `SpeakingCoachTests` pins whitespace-tolerant verbatim quotes, score bounds, the no-IELTS-band guard, and the evaluator evidence envelope. `WritingServiceTests` pins union/collision behavior and exact shared top-error semantics. |
| Azure adapter parsing and aggregation | Automated + captured response | Adapter tests pin configuration modes, continuous paragraph aggregation, error mapping, and parsing against the unmodified real Azure JSON resource `azure-reading-response.json`. No live call runs in CI. |
| Speech live provider flow | Runtime verified | Dev-only E2E on 2026-07-11 with real Azure eastasia credentials and 16-kHz spoken WAVs: card read returned 92 accuracy / 100 fluency / 90.3 prosody; speaking returned a transcript plus 92 pronunciation / 92.29 fluency / 89.7 prosody, persisted provider/model metadata, logged a one-minute Speaking PRACTICE session, and surfaced its verb-form error through local MCP `grammar_weaknesses`. Fixtures were removed afterwards. |
| /study page and Capture study controls | Runtime verified | Playwright walkthroughs on 2026-07-11 at 1440x900: Log tab, Vocabulary stats/table and mic dialog including headless mic error, Writing detail, and Speaking stats/question/recorder/history/detail with network-mocked speech results; a fresh session reported zero console errors/warnings. Capture chips and unified Recent rows remain covered by the earlier walkthrough. |
| Live model grading agreement | Gap | No calibration set of officially-scored essays is wired into an opt-in evaluation; agreement numbers come from the literature (see decision 0016), not from this codebase. |
