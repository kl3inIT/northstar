/**
 * Projects — the LDP execution layer. A project is a large, staged piece of
 * work under a discipline (a scholarship application, an exam campaign),
 * moving through named milestones; tasks can attach to one as its agenda.
 * Progress is always derived from milestones, never stored.
 */
package com.northstar.core.project;
