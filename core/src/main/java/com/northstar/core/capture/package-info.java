/**
 * AI Capture: raw one-sentence capture is preserved, then parsed into entities (Task, Expense, StudyLog, HabitLog, Learning, ...). Single write-path for humans (web/Telegram) and agents (MCP).
 */
package com.northstar.core.capture;
