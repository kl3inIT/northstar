package com.northstar.core.shared;

/**
 * The shared display palette — used by calendar events and disciplines (and
 * later habits). Values mirror the CHECK constraints in V6/V7.
 */
public enum ColorName {
    BLUE, GREEN, RED, YELLOW, PURPLE, ORANGE, GRAY
}
